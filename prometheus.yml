global:
  scrape_interval: 15s      # Recolectar metricas cada 15 segundos
  evaluation_interval: 15s

scrape_configs:
  - job_name: "catalog"
    static_configs:
      - targets: ["catalog:8000"]   # El servicio catalog expone /metrics
    metrics_path: "/metrics"
